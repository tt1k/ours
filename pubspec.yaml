name: ours
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=2.18.4 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.5
  cached_network_image: ^3.2.2
  fluttertoast: ^8.1.1
  toggle_rotate: ^1.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - image/
  fonts:
    - family: oursFont
      fonts:
        - asset: image/ZiXinFangXiaMuTi.ttf
  
module:
    androidX: true
